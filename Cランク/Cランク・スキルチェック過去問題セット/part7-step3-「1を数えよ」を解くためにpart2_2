<?php
    // 自分の得意な言語で
    // Let's チャレンジ！！
    $S = fgets(STDIN);
    $oneCount = 0;
    for ($i = 0; $i < mb_strlen($S); $i++) {
        // 文字列は配列のようにアクセスできる
        if ($S[$i] == 1) {
         $oneCount++;
        }
    }
    echo "$oneCount\n";
?>